### Экзамен

Реализовать корзину, оформление заказа и сохранение корзины в БД. Корзина хранится минимально: только `id` товара, размер и количество, а отображаемое содержимое собирается из данных каталога и локально сохранённых данных.

**Сборка**
* Проект собирается без ошибок

**Бейдж**
* На иконке корзины в нижней навигации отображается количество товаров в корзине
* Бейдж обновляется при изменении содержимого корзины

**Добавление в корзину**
* Кнопка «В корзину» в bottom sheet деталей работает
* В корзину добавляется выбранный размер
* Повторное добавление того же товара с тем же размером увеличивает количество (а не создаёт дубликат)

**Экран корзины**
* Список добавленных товаров
* Элемент корзины содержит:
  * Название товара (`name`)
  * Размер (`sizes[].name`)
  * Цена с учётом количества (`priceInKopecks` × количество) — отформатирована в рублях
  * Контрол изменения количества (+/−)
  * Кнопка удаления позиции
* Общая стоимость корзины
* Кнопка очистки корзины → модальное окно подтверждения
* Empty state если корзина пустая
* Корзина хранится в БД (Room / CoreData / Hive / Drift / аналог) — не SharedPreferences
* В БД сохраняются только `id` товара, `id` размера и количество
* Корзина при отображении собирается из каталога и локально сохранённых данных

**Оформление заказа на экране корзины**
* Кнопка «Оформить»
* Поля для ввода имени, email и комментария
* Валидация имени: непустая строка
* Валидация email: простое соответствие структуре email
* Кнопка «Оформить» отключена, если не заполнено имя или email невалидный
* После оформления: очистка корзины + bottom sheet / alert с сообщением об успешном оформлении

**Git**